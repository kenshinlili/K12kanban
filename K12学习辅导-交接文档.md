# K12 学习看板 — 云→本地 交接文档

> **交接时间**：2026-09-04 19:00
> **交接方向**：WorkBuddy 云沙箱任务 → 本地任务（Windows `D:\AI\WikiProjects\K12学习辅导`）
> **用途**：把这篇内容整段复制/转发到「本地任务」对话框，本地 agent 读完后即可直接接手开发。

---

## 一、项目是什么

给**四年级学生「李迦一」**（2016 年生，语文/数学/英语三科）做的**家庭学习打卡看板**。

核心玩法：

1. 家长每天在 8 个「看板」（如语文-听写、语文-作文、数学-校内）上打卡
2. 拍照上传作业 → AI 识别错题 → 生成错题条目
3. 家长复核确认 → 错题进入艾宾浩斯复习调度队列
4. 孩子端看「今日任务」和「知识点热力图」，家长端看统计和待复核

**线上地址**（已发布，仍在运行）：`https://ad6530902b1e2c53a.app.workbuddy.link`

---

## 二、当前分工（重要，别搞混）

| 环节 | 在哪里 | 说明 |
|---|---|---|
| **代码维护** | 🖥️ 本地 `D:\AI\WikiProjects\K12学习辅导\` | 你在本地改代码 |
| **代码版本** | ☁️ GitHub（建议建仓） | 本地 `git push` ↔ 云沙箱 `git pull` |
| **网站运行** | ☁️ `app.workbuddy.link` | Flask 应用跑在 WorkBuddy 云沙箱，**不迁到本地** |
| **数据存储** | ☁️ 运行实例的本地磁盘 | SQLite + uploads 目录（见下方「风险」） |

> ⚠️ 「云端运行」指的是**网站部署在云上**，不是把 Flask 实例搬到你 Windows 上跑。本地只管写代码，部署由云沙箱任务执行 `publish.js` 完成。

---

## 三、本地目录结构

```
D:\AI\WikiProjects\K12学习辅导\
├── README.md                    ← 知识库首页（从这里开始读）
├── CHANGELOG.md                 ← 版本记录（当前 v1.1）
├── 本地开发指南.md               ← 本地怎么跑起来、常见问题
├── start.bat                    ← Windows 双击启动
├── kanban\                      ← ★ 项目源码（真正要改的代码在这）
│   ├── app.py                   ← Flask 主程序（57K）
│   ├── db.py                    ← 数据库封装
│   ├── requirements.txt         ← Flask>=2.3.0, pypinyin>=0.49.0
│   ├── 使用说明.md
│   ├── static\
│   │   ├── index.html
│   │   ├── app.js               ← 前端主逻辑（53K）
│   │   └── style.css
│   ├── data\kanban.db           ← SQLite 数据库（92K）
│   └── uploads\                 ← 作业照片（当前 2 张 jpg）
├── 00-快速上手\
│   ├── 系统访问与角色切换.md
│   └── 给迦一的用法.md
├── 01-产品设计\
│   ├── 需求背景与目标.md
│   ├── 核心设计决策.md           ← ⚠️ 决策1 已标注「落地状态：未完成」
│   └── 页面与功能地图.md
├── 02-系统实现\
│   ├── 技术架构.md
│   ├── 数据模型.md               ← 11 张表的定义
│   ├── API清单.md                ← 所有接口
│   └── 部署与运维.md             ← ⚠️ 含「当前无自动异地备份」章节
├── 03-机制说明\
│   ├── 复习调度算法.md
│   ├── 撤回机制.md
│   └── 知识点热力图与学习计划.md
├── 04-操作手册\
│   └── 使用说明.md               ← 给家长看的（v1.1 已修正）
├── 05-踩坑与经验\
│   └── 踩坑记录.md
└── 06-待办与规划\
    ├── 当前待办.md               ← ★ 优先读这个
    └── 后续路线图.md
```

**给本地 agent 的话**：接手后先读 `README.md` → `06-待办与规划\当前待办.md` → `02-系统实现\数据模型.md`，这三篇读完就能干活。

---

## 四、本地怎么跑起来

```bat
:: 方式一：双击
start.bat

:: 方式二：命令行
cd D:\AI\WikiProjects\K12学习辅导\kanban
pip install -r requirements.txt
python app.py
```

然后浏览器打开 `http://localhost:3000`

**已验证**：代码无任何硬编码绝对路径（全部用 `os.path.dirname(os.path.abspath(__file__))`），Windows 可直接运行。端口通过 `PORT` 环境变量读取，默认 3000。

---

## 五、技术栈与数据

- **后端**：Python 3.11 + Flask，SQLite（单文件）
- **前端**：原生 HTML/JS/CSS，无构建步骤
- **依赖**：只有 `Flask` 和 `pypinyin` 两个

**11 张表**（当前数据量）：

| 表 | 行数 | 作用 |
|---|---|---|
| `members` | 3 | 爸爸/妈妈/李迦一 |
| `boards` | 8 | 打卡看板（语文3+数学1+英语…） |
| `knowledge_points` | 51 | 知识点（用于热力图） |
| `checkins` | 2 | 打卡记录 |
| `photos` | 1 | 作业照片 |
| `ai_runs` | 1 | AI 识别批次 |
| `wrong_questions` | 3 | 错题（全部已确认） |
| `review_schedules` | 3 | 复习计划 |
| `review_actions` | 21 | 操作时间线（撤回机制的基础） |
| `review_rules` | 3 | 复习间隔规则 |
| `ai_runs` 等 | — | — |

**现有业务数据**（2026-09-03 语文听写）：
- 打卡 1 条：`[第一单元]《观潮》词语听写`，20 分钟，状态 confirmed
- AI 识别：3 处错题（奇观/风平浪静/人声鼎沸），全部已由爸爸确认
- 照片 1 张：`92c1759056a84b38b363880b67ee531b.jpg`（原始名 `hw0903.jpg`）

---

## ⚠️ 六、必须知道的风险和已修正的错误

### 1. 数据没有自动备份（最大风险）

`app.py` 里**没有任何** `tdrive` / `netdrive` / 云盘 / COS 调用。照片走的是：

```python
f.save(os.path.join(UPLOAD_DIR, fname))   # 只存到服务器本地 uploads\
```

数据库和照片**都只在运行实例的本地磁盘上**，环境一旦重置/回收就没了。

> **这件事之前在文档里写错过**。v1.0 的使用说明写着「照片上传到项目云盘，两者都已闭环，不会丢」——**这是错的**，v1.1 已修正为「⚠️ 目前有风险，尚未闭环」。交接时请以此为准。

### 2. 项目资产（云盘）里的照片是手动上传的，不是系统自动的

WorkBuddy 项目资产 `李迦一K12\四年级上\语文\作业归档\听写\2026-09-03\` 下有 2 张图（`9.3_听写错题_第1页/第2页.jpg`）。

**已核实是手动归档**，证据：
- 两张图 md5 不同（`0543de53…` / `b28741df…`），与沙箱 `uploads\` 里那张（`3527bd79…`）也不同
- 文件创建时间（9/3 19:54、9/4 10:13）**早于**目录创建时间（9/4 10:33），是"先传文件、后建目录、再移动"的痕迹
- 截图显示更新人为「金金金」（用户本人）

### 3. 应用有效期是 5 年，不是 30 天

`publish.js` 里明确写着 `DEFAULT_EXPIRE_SECONDS = 157680000`（≈5 年），发布请求带 `"persistent": true`。
之前流传的「30 天不活跃回收」是另一条通道（纯静态站一键部署），**不适用于本项目**。

---

## 七、待办清单（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 🔥 P0 | **照片 + 数据库自动备份到项目资产** | 在 `/api/photos` 保存本地后，调 tdrive 上传；`kanban.db` 定期同步 |
| P1 | **打通 GitHub 代码联动** | 本地 push → 云沙箱 pull → publish.js 发布。当前云沙箱 `gh` 未登录，需走 connector 取 token |
| P2 | 错题批量操作（多选确认/删除） | 错题多了以后逐条点太累 |
| P3 | 数据导出（错题本导出 PDF/打印） | 家长想打印纸质错题本 |
| P3 | 移动端拍照体验优化 | 目前手机上传体验一般 |

---

## 八、给本地 agent 的接手指令（可直接复制）

```
我已把 K12 学习看板项目交接过来，代码在 D:\AI\WikiProjects\K12学习辅导\

请先按顺序读这三个文件，然后向我确认你理解了项目：
1. D:\AI\WikiProjects\K12学习辅导\README.md
2. D:\AI\WikiProjects\K12学习辅导\06-待办与规划\当前待办.md
3. D:\AI\WikiProjects\K12学习辅导\02-系统实现\数据模型.md

项目背景：给四年级学生李迦一做的家庭学习打卡看板。
Python 3.11 + Flask + SQLite，前端原生 JS，无构建步骤。
线上跑在 https://ad6530902b1e2c53a.app.workbuddy.link（云端部署，不迁本地）。

当前最重要的待办是：实现「作业照片 + 数据库自动备份到 WorkBuddy 项目资产」，
因为现在数据只存在服务器本地磁盘，没有异地备份。

读完后告诉我：你打算怎么实现这个备份功能，需要我提供什么信息。
```

---

## 九、云沙箱侧的现状（供参考，本地 agent 不用管）

- 运行环境：CloudStudio 沙箱（`cs-spacelet-v2-cpu-556dcdd468-g9w8w`），256G 磁盘 / 8G 内存 / 4 核
- 应用进程：`python3.11 app.py` 监听 3000 端口，PID 124555，运行正常
- 发布通道：`publish.js` → `agentos artifact-releases`，`expire=157680000`
- 项目资产（云盘）：10.00 GB，已用 8.48 MB
- 沙箱暂未登录 GitHub，需 `source /root/.codebuddy/skills/github-connector/scripts/get_token.sh github` 取 token

---

## 十、一句话总结

> 代码搬到本地 `D:\AI\WikiProjects\K12学习辅导\` 维护，用 GitHub 做版本中转，网站继续部署在 `app.workbuddy.link`。**当前最大风险是数据没备份**，这是本地接手后要做的第一件事。
