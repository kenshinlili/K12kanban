# 💻 本地开发指南

> 归属：K12学习辅导
> 最后更新：2026-09-04 · v1.0
> [← 返回首页](README.md)

这份指南帮你（或接手的 AI）在**本机 Windows** 上把看板跑起来、继续开发。

---

## 1. 前置条件

只需要 **Python 3.11**（代码用了 3.11 语法特性，3.9 以下不行）。

检查是否已装：

```cmd
python --version
```

若没装，去 [python.org](https://www.python.org/downloads/) 下载，**安装时务必勾选 `Add Python to PATH`**。

---

## 2. 三步启动

### 方式 A：双击启动（推荐日常使用）

双击根目录下的 **`start.bat`** → 自动装完依赖后访问 **http://localhost:3000**

> 停止服务：关闭那个黑窗口，或在窗口里按 `Ctrl+C`。

### 方式 B：命令行

```cmd
cd D:\AI\WikiProjects\K12学习辅导\kanban

pip install -r requirements.txt     :: 首次运行需要
python app.py
```

---

## 3. 目录说明

```
K12学习辅导/
├── README.md            知识库首页（文档导航）
├── start.bat            Windows 双击启动
├── 本地开发指南.md       本文件
├── kanban/              项目源码
│   ├── app.py           主应用：路由 + 业务逻辑（57KB）
│   ├── db.py            数据库连接 + SCHEMA 定义
│   ├── requirements.txt 依赖清单
│   ├── static/          前端（无构建步骤，改完刷新即可）
│   │   ├── index.html
│   │   ├── app.js       前端全部逻辑（53KB）
│   │   └── style.css
│   ├── data/kanban.db   SQLite 数据库（含当前真实数据）
│   └── uploads/         上传的作业照片
└── 00-快速上手/ … 06-待办与规划/   知识库文档
```

### 关键：路径完全可移植

代码中**没有任何硬编码的绝对路径**，全部基于脚本自身位置定位：

```python
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DB_PATH  = os.path.join(BASE_DIR, 'data', 'kanban.db')
UPLOAD_DIR = os.path.join(BASE_DIR, 'uploads')
port = int(os.environ.get('PORT', 3000))
```

所以整个 `kanban/` 目录**拷到任何位置、任何系统都能直接跑**。

---

## 4. 日常开发流程

| 改了什么 | 要做什么 |
|---|---|
| `static/` 下的 HTML/CSS/JS | **不用重启**，浏览器硬刷新（`Ctrl+F5`）即可 |
| `app.py` / `db.py` | **必须重启服务**（关掉黑窗口重新双击 start.bat） |
| 表结构（SCHEMA） | 重启后会自动建新表；已有表不会自动改结构，需手动迁移 |

### 改 JS 后必做语法校验

```cmd
cd kanban
node --check static/app.js
```

> 没装 Node 也可以跳过，但改完 `app.js` 建议在浏览器 F12 看一下 Console 有没有报错。

### 冒烟自查清单

改完代码建议过一遍：

1. 📋 今日看板能打卡、能传照片
2. 🔔 待处理显示正确的 3 类状态
3. 🔁 复习能判对/错、能 `↩ 撤销本次`
4. ❌ 错题本三层展示正常
5. 🧠 知识点三个子页签（台账/热力图/学习计划）都能渲染

---

## 5. 数据库

### 位置

`kanban\data\kanban.db`（单文件，直接复制即备份）

### 备份

```cmd
copy kanban\data\kanban.db kanban\data\kanban.db.bak
```

### 直查

```cmd
cd kanban
python -c "import db; c=db.get_conn(); [print(dict(r)) for r in c.execute('SELECT * FROM wrong_questions')]; c.close()"
```

### 常用调参 SQL

```sql
-- 语文：第 2/7/30/30 天复习
UPDATE review_rules SET intervals='[2,7,30,30]' WHERE subject='语文';
-- 数学：只练 2 次，间隔拉长
UPDATE review_rules SET intervals='[1,30]', max_stages=2 WHERE subject='数学';
```

> 改完只影响**之后**新排的日期，已生成的 `next_review_date` 不会变。

---

## 6. 常见问题

**Q: 提示"端口 3000 已被占用"**
A: 换端口启动：`set PORT=3001 && python app.py`

**Q: 中文显示成乱码**
A: start.bat 里已设 `chcp 65001`。手动命令行启动的话，先执行 `chcp 65001`。

**Q: `pip install pypinyin` 失败**
A: 换国内源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

**Q: 照片显示不出来**
A: 检查 `kanban\uploads\` 下文件是否存在。照片是本地相对路径存储的，目录整体拷贝才不会丢。

**Q: 改了代码没生效**
A: 前端改完要 `Ctrl+F5` 硬刷新；Python 改完必须重启服务。

**Q: `python` 命令找不到**
A: 安装时没勾 `Add Python to PATH`。重新安装并勾选，或改用 `py -3 app.py`。

---

## 7. 当前数据状态（2026-09-04）

| 表 | 行数 |
|---|---|
| boards | 8 |
| knowledge_points | 51 |
| members | 3 |
| checkins | 2 |
| wrong_questions | 3 |
| review_schedules | 3 |
| review_actions | 21 |
| ai_runs | 1 |

**3 道测试错题**（语文·听写·第一单元，已带拼音）：奇观 / 风平浪静 / 人声鼎沸。

> 想清空重来：删掉 `kanban\data\kanban.db`，重启服务会自动重建空库。

---

## 8. 与线上版本的关系

| | 线上正式版 | 本地开发版 |
|---|---|---|
| 地址 | https://ad6530902b1e2c53a.app.workbuddy.link | http://localhost:3000 |
| 数据 | 云端，三人实时同步 | 本地 `kanban.db`，**独立数据** |
| 用途 | 日常使用 | 开发调试 |

⚠️ **两边数据不互通**。本地开发时随便造数据、随便改，不会影响线上。调好了再同步代码到云端发布。

---

## 相关

- [技术架构](02-系统实现/技术架构.md)
- [部署与运维](02-系统实现/部署与运维.md)
- [踩坑记录](05-踩坑与经验/踩坑记录.md)
- [后续路线图](06-待办与规划/后续路线图.md)
